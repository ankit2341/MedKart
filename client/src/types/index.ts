export * from "./product";
export * from "./user";

export enum AppOrganization {
  Name = "MedKart",
}

export enum AppStaticPath {
  Home = "/",
  Products = "/products",
  Profile = "/profile",
  Offers = "/offers",
  BookLabTest = "/book-lab-test",
}
